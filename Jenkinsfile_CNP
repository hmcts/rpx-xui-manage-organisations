#!groovy

properties([
    [
        $class       : 'GithubProjectProperty',
        displayName  : 'Expert UI Manage Organisations',
        projectUrlStr: 'https://github.com/hmcts/rpx-xui-manage-organisations'
    ],
    pipelineTriggers([
        [$class: 'GitHubPushTrigger']
    ]),
    parameters([
        booleanParam(
            name: 'RUN_PLAYWRIGHT_ACCESSIBILITY',
            defaultValue: false,
            description: 'Manual-only: run the non-blocking unified Playwright accessibility pack on this CNP build.'
        ),
        booleanParam(
            name: 'RUN_PLAYWRIGHT_A11Y',
            defaultValue: false,
            description: 'Compatibility switch: runs the unified Playwright accessibility pack.'
        ),
        booleanParam(
            name: 'RUN_PLAYWRIGHT_WAVE_A11Y',
            defaultValue: false,
            description: 'Compatibility switch: runs the unified Playwright accessibility pack.'
        )
    ])
])

@Library("Infrastructure@DTSPO-30107-jenkins-agents")

//To enable PACT testing on pipeline uncomment the next line
import uk.gov.hmcts.contino.AppPipelineDsl

def type      = "nodejs"
def product   = "xui"
def component = "mo-webapp"
def channel   = '#xui-pipeline'
def yarnBuilder = new uk.gov.hmcts.contino.YarnBuilder(this)

def playwrightBrowsersEnv = {
    ["PLAYWRIGHT_BROWSERS_PATH=${env.WORKSPACE}/.cache/ms-playwright"]
}

def handleParallelInterruption = { Exception e ->
    def interrupted = false
    def failFastExitCodes = ['129', '137', '143']
    if (e instanceof org.jenkinsci.plugins.workflow.steps.FlowInterruptedException) {
        def msg = e.getMessage() ?: ''
        interrupted = msg.contains('Failed in branch')
        echo "[parallel] FlowInterruptedException: message=${msg}"
    } else if (e instanceof hudson.AbortException) {
        def msg = e.getMessage() ?: ''
        def matcher = (msg =~ /(exit code|status)\s+(\d+)/)
        def exitCode = matcher.find() ? matcher.group(2) : null
        interrupted = (currentBuild?.result == 'ABORTED') || (exitCode in failFastExitCodes)
        echo "[parallel] AbortException: buildResult=${currentBuild?.result}, exitCode=${exitCode}"
    } else {
        echo "[parallel] Non-abort exception: ${e.getClass().getName()}: ${e.getMessage()}"
    }
    if (interrupted) {
        echo "[parallel] Marking stage as ABORTED due to Jenkins interruption"
        catchError(buildResult: 'ABORTED', stageResult: 'ABORTED') { throw e }
        return true
    }
    echo "[parallel] Not an interruption; rethrowing"
    return false
}

def ensureHtmlReportForPublishing = { String reportDir, String reportFile, String suiteName ->
    withEnv([
        "PW_ODHIN_ENSURE_REPORT_DIR=${reportDir}",
        "PW_ODHIN_ENSURE_REPORT_FILE=${reportFile}",
        "PW_ODHIN_ENSURE_SUITE_NAME=${suiteName}"
    ]) {
        steps.sh '''
            node scripts/ensure-odhin-report.js \
              --report-dir "$PW_ODHIN_ENSURE_REPORT_DIR" \
              --report-file "$PW_ODHIN_ENSURE_REPORT_FILE" \
              --suite-name "$PW_ODHIN_ENSURE_SUITE_NAME"
        '''
    }
}

def stagePlaywrightArtifacts = { String stageDir, String sourceDir ->
    withEnv([
        "PW_ARTIFACT_STAGE_DIR=${stageDir}",
        "PW_ARTIFACT_SOURCE_DIR=${sourceDir}"
    ]) {
        steps.sh '''
            set -eu
            rm -rf "$PW_ARTIFACT_STAGE_DIR"
            mkdir -p "$PW_ARTIFACT_STAGE_DIR"

            if [ -d "$PW_ARTIFACT_SOURCE_DIR" ]; then
              source_abs="$(cd "$PW_ARTIFACT_SOURCE_DIR" && pwd)"
              file_list="$(mktemp)"
              find "$source_abs" -path '*/.playwright-artifacts-*' -prune -o -type f -print 2>/dev/null > "$file_list"
              copied=0
              failed=0
              while IFS= read -r file; do
                case "$file" in
                  *.png|*.webm|*.zip|*/error-context.md|*/node-api-calls.json|*/node-api-calls.pretty.txt)
                    if [ ! -f "$file" ]; then
                      failed=$((failed + 1))
                      continue
                    fi
                    relative="${file#$source_abs/}"
                    target="$PW_ARTIFACT_STAGE_DIR/$relative"
                    mkdir -p "$(dirname "$target")"
                    if cp "$file" "$target"; then
                      copied=$((copied + 1))
                    else
                      failed=$((failed + 1))
                    fi
                    ;;
                esac
              done < "$file_list"
              rm -f "$file_list"
              echo "Staged $copied Playwright artifact(s) from $PW_ARTIFACT_SOURCE_DIR to $PW_ARTIFACT_STAGE_DIR"
              if [ "$failed" -gt 0 ]; then
                echo "Failed to stage $failed Playwright artifact(s) from $PW_ARTIFACT_SOURCE_DIR"
                exit 1
              fi
            fi
        '''
    }
}

def publishPlaywrightJUnit = { String testResults ->
    steps.junit(
        allowEmptyResults: false,
        testResults: testResults
    )
}

def publishManageOrgEvidenceDashboard = { String reportName ->
    withEnv([
        "MANAGE_ORG_EVIDENCE_TITLE=${reportName}"
    ]) {
        steps.sh 'node scripts/build-playwright-evidence-dashboard.js --title "$MANAGE_ORG_EVIDENCE_TITLE"'
    }
    publishHTML([
        allowMissing         : true,
        alwaysLinkToLastBuild: true,
        keepAll              : true,
        reportDir            : "functional-output/tests/manage-org-evidence",
        reportFiles          : 'index.html',
        reportName           : reportName
    ])
    steps.archiveArtifacts(
        allowEmptyArchive: true,
        artifacts: 'functional-output/tests/manage-org-evidence/**'
    )
}

def publishPlaywrightE2EReport = { String reportName ->
    ensureHtmlReportForPublishing(
        "functional-output/tests/playwright-e2e/odhin-report",
        'xui-playwright-e2e.html',
        reportName
    )
    publishHTML([
        allowMissing         : true,
        alwaysLinkToLastBuild: true,
        keepAll              : true,
        reportDir            : "functional-output/tests/playwright-e2e/odhin-report",
        reportFiles          : 'xui-playwright-e2e.html',
        reportName           : reportName
    ])
    stagePlaywrightArtifacts('functional-output/tests/playwright-e2e/stable-artifacts', 'test-results/playwright-e2e')
    publishPlaywrightJUnit('functional-output/tests/playwright-e2e/**/*junit.xml')
    steps.archiveArtifacts(
        allowEmptyArchive: true,
        artifacts: 'functional-output/tests/playwright-e2e/**'
    )
}

def publishPlaywrightApiReport = { String reportName ->
    ensureHtmlReportForPublishing(
        "functional-output/tests/playwright-api/odhin-report",
        'xui-mo-playwright-api.html',
        reportName
    )
    publishHTML([
        allowMissing         : true,
        alwaysLinkToLastBuild: true,
        keepAll              : true,
        reportDir            : "functional-output/tests/playwright-api/odhin-report",
        reportFiles          : 'xui-mo-playwright-api.html',
        reportName           : reportName
    ])
    stagePlaywrightArtifacts('functional-output/tests/playwright-api/stable-artifacts', 'test-results/playwright-api')
    publishPlaywrightJUnit('functional-output/tests/playwright-api/**/*junit.xml')
    steps.archiveArtifacts(
        allowEmptyArchive: true,
        artifacts: 'functional-output/tests/playwright-api/**'
    )
}

def publishPlaywrightIntegrationReport = { String reportName ->
    ensureHtmlReportForPublishing(
        "functional-output/tests/playwright-integration/odhin-report",
        'xui-mo-playwright-integration.html',
        reportName
    )
    publishHTML([
        allowMissing         : true,
        alwaysLinkToLastBuild: true,
        keepAll              : true,
        reportDir            : "functional-output/tests/playwright-integration/odhin-report",
        reportFiles          : 'xui-mo-playwright-integration.html',
        reportName           : reportName
    ])
    stagePlaywrightArtifacts('functional-output/tests/playwright-integration/stable-artifacts', 'test-results/playwright-integration')
    publishPlaywrightJUnit('functional-output/tests/playwright-integration/**/*junit.xml')
    steps.archiveArtifacts(
        allowEmptyArchive: true,
        artifacts: 'functional-output/tests/playwright-integration/**'
    )
}

def publishPlaywrightAccessibilityReport = { String reportName ->
    try {
        ensureHtmlReportForPublishing(
            "functional-output/tests/playwright-accessibility/odhin-report",
            'xui-playwright-accessibility.html',
            reportName
        )
        publishHTML([
            allowMissing         : true,
            alwaysLinkToLastBuild: true,
            keepAll              : true,
            reportDir            : "functional-output/tests/playwright-accessibility/odhin-report",
            reportFiles          : 'xui-playwright-accessibility.html',
            reportName           : reportName
        ])
    } catch (Exception e) {
        echo "[playwright-accessibility] Report publish failed but is non-blocking: ${e.getClass().getName()}: ${e.getMessage()}"
    }
    try {
        stagePlaywrightArtifacts('functional-output/tests/playwright-accessibility/stable-artifacts', 'test-results/playwright-accessibility')
        catchError(buildResult: 'SUCCESS', stageResult: 'UNSTABLE') {
            publishPlaywrightJUnit('functional-output/tests/playwright-accessibility/**/*junit.xml')
        }
        steps.archiveArtifacts(
            allowEmptyArchive: true,
            artifacts: 'functional-output/tests/playwright-accessibility/**'
        )
    } catch (Exception e) {
        echo "[playwright-accessibility] Evidence publish failed but is non-blocking: ${e.getClass().getName()}: ${e.getMessage()}"
    }
}

def runPlaywrightFunctionalTestsInParallel = { String environmentName ->
    stage("${environmentName} Functional Tests (Parallel)") {
        try {
            def functionalSuites = [
                failFast: false,
                api: {
                    stage('API Tests') {
                        try {
                            withEnv(playwrightBrowsersEnv() + [
                                "FUNCTIONAL_TESTS_WORKERS=6",
                                "PLAYWRIGHT_TEST_OUTPUT_DIR=test-results/playwright-api",
                                "PLAYWRIGHT_HTML_OUTPUT=functional-output/tests/playwright-api/html-report",
                                "PLAYWRIGHT_JUNIT_OUTPUT=functional-output/tests/playwright-api/playwright-api-junit.xml",
                                "PLAYWRIGHT_SKIP_INSTALL=true"
                            ]) {
                                yarnBuilder.yarn('test:api:pw:raw')
                            }
                        } catch (Exception e) {
                            echo "[parallel] API Tests catch: ${e.getClass().getName()}: ${e.getMessage()}"
                            if (handleParallelInterruption(e)) { return }
                            throw e
                        } finally {
                            publishPlaywrightApiReport("${environmentName} Manage Org Playwright API")
                        }
                    }
                },
                playwrightIntegration: {
                    stage('Playwright Integration Tests') {
                        try {
                            withEnv(playwrightBrowsersEnv() + [
                                "FUNCTIONAL_TESTS_WORKERS=6",
                                "PLAYWRIGHT_TEST_OUTPUT_DIR=test-results/playwright-integration",
                                "PLAYWRIGHT_JUNIT_OUTPUT=functional-output/tests/playwright-integration/playwright-integration-junit.xml",
                                "PLAYWRIGHT_SKIP_INSTALL=true"
                            ]) {
                                yarnBuilder.yarn('test:playwright:integration:raw')
                            }
                        } catch (Exception e) {
                            echo "[parallel] Playwright Integration Tests catch: ${e.getClass().getName()}: ${e.getMessage()}"
                            if (handleParallelInterruption(e)) { return }
                            throw e
                        } finally {
                            publishPlaywrightIntegrationReport("${environmentName} Manage Org Playwright Integration")
                        }
                    }
                },
                playwrightE2E: {
                    stage('Playwright E2E') {
                        try {
                            withEnv(playwrightBrowsersEnv() + [
                                "FUNCTIONAL_TESTS_WORKERS=6",
                                "PLAYWRIGHT_TEST_OUTPUT_DIR=test-results/playwright-e2e",
                                "PLAYWRIGHT_JUNIT_OUTPUT=functional-output/tests/playwright-e2e/playwright-e2e-junit.xml",
                                "PW_SESSION_CAPTURE_FAILURE_TTL_MS=0",
                                "PLAYWRIGHT_TAGS=@e2e",
                                "PLAYWRIGHT_EXCLUDE_TAGS=@a11y",
                                "PLAYWRIGHT_SKIP_INSTALL=true"
                            ]) {
                                yarnBuilder.yarn('test:playwrightE2E:raw')
                            }
                        } catch (Exception e) {
                            echo "[parallel] Playwright E2E catch: ${e.getClass().getName()}: ${e.getMessage()}"
                            if (handleParallelInterruption(e)) { return }
                            throw e
                        } finally {
                            publishPlaywrightE2EReport("${environmentName} Manage Org Playwright E2E")
                        }
                    }
                }
            ]

            if (params.RUN_PLAYWRIGHT_ACCESSIBILITY || params.RUN_PLAYWRIGHT_A11Y || params.RUN_PLAYWRIGHT_WAVE_A11Y) {
                functionalSuites.playwrightAccessibility = {
                    stage('Playwright Accessibility Tests') {
                        try {
                            withEnv(playwrightBrowsersEnv() + [
                                "FUNCTIONAL_TESTS_WORKERS=6",
                                "PLAYWRIGHT_TEST_OUTPUT_DIR=test-results/playwright-accessibility",
                                "PLAYWRIGHT_JUNIT_OUTPUT=functional-output/tests/playwright-accessibility/playwright-accessibility-junit.xml",
                                "PW_SESSION_CAPTURE_FAILURE_TTL_MS=0",
                                "PLAYWRIGHT_SKIP_INSTALL=true"
                            ]) {
                                yarnBuilder.yarn('test:accessibility:playwright')
                            }
                        } catch (Exception e) {
                            echo "[parallel-report-gathering] Playwright Accessibility failed but is non-blocking: ${e.getClass().getName()}: ${e.getMessage()}"
                            if (handleParallelInterruption(e)) { return }
                        } finally {
                            publishPlaywrightAccessibilityReport("${environmentName} Manage Org Playwright Accessibility")
                        }
                    }
                }
            } else {
                echo '[playwright-accessibility] CNP stage omitted by default. Set RUN_PLAYWRIGHT_ACCESSIBILITY=true on a manual build to include this pack.'
            }

            parallel(functionalSuites)
        } finally {
            publishManageOrgEvidenceDashboard("${environmentName} Manage Org Playwright Evidence")
        }
    }
}

def secrets = [
  'rpx-${env}': [
    secret('test-user1-email', 'TEST_USER1_EMAIL'),
    secret('test-user1-password', 'TEST_USER1_PASSWORD'),
    secret('test-roo-username', 'TEST_ROO_EMAIL'),
    secret('test-roo-password', 'TEST_ROO_PASSWORD'),
    secret('test-user2-email', 'TEST_USER2_EMAIL'),
    secret('test-user2-password', 'TEST_USER2_PASSWORD'),
    secret('test-townley-email', 'TEST_TOWNLEY_EMAIL'),
    secret('test-townley-password', 'TEST_TOWNLEY_PASSWORD'),
    secret('test-api-email', 'TEST_API_EMAIL'),
    secret('test-api-password', 'TEST_API_PASSWORD'),
    secret('mo-s2s-client-secret', 'S2S_SECRET'),
    secret('xui-oauth2-token', 'IDAM_SECRET'),
    secret('appinsights-connection-string-mo', 'APPINSIGHTS_CONNECTION_STRING'),
    secret('google-analytics-key', 'GOOGLE_ANALYTICS_KEY'),
    secret('launch-darkly-client-id', 'LAUNCH_DARKLY_CLIENT_ID'),
    secret('system-user-name', 'SYSTEM_USER_NAME'),
    secret('system-user-password', 'SYSTEM_USER_PASSWORD'),
    secret('mo-webapp-redis6-connection-string', 'REDISCLOUD_URL'),

  ],
]

def branchesToSync = ['demo', 'perftest', 'ithc']

static LinkedHashMap<String, Object> secret(String secretName, String envVar) {
    [ $class: 'AzureKeyVaultSecret',
      secretType: 'Secret',
      name: secretName,
      version: '',
      envVariable: envVar
    ]
}

withPipeline(type, product, component) {
    disableCleanupOfHelmReleaseOnFailure()
    enableSlackNotifications(channel)
    loadVaultSecrets(secrets)
    enableAksStagingDeployment()
    syncBranchesWithMaster(branchesToSync)

   enablePactAs([
     AppPipelineDsl.PactRoles.CONSUMER
])

    afterSuccess('checkout') {
        stage('Yarn Install') {
            withEnv(playwrightBrowsersEnv()) {
                yarnBuilder.yarn('install --immutable')
                sh 'touch .yarn_dependencies_installed'
            }
        }
    }

    afterAlways('checkout') {
        stage('Playwright Install Chromium') {
            try {
                withEnv(playwrightBrowsersEnv()) {
                    sh '''
                        mkdir -p "$PLAYWRIGHT_BROWSERS_PATH"
                        if [ ! -f .yarn_dependencies_installed ]; then
                          node .yarn/releases/yarn-4.9.2.cjs install --immutable
                          touch .yarn_dependencies_installed
                        fi
                        node .yarn/releases/yarn-4.9.2.cjs test:setup:playwright-install-chromium
                        node .yarn/releases/yarn-4.9.2.cjs test:setup:playwright-verify-chromium
                    '''
                }
            } catch (Error) {
                error(message: "${STAGE_NAME} is unstable: " + Error.toString())
            }
        }
    }

    afterSuccess('build') {
        yarnBuilder.yarn('build')
    }

    afterSuccess('test') {
        stage('API Unit Coverage') {
            yarnBuilder.yarn('test:coverage:node')
        }

        publishHTML ( [
            allowMissing         : true,
            alwaysLinkToLastBuild: true,
            keepAll              : true,
            reportDir            : "reports/tests/coverage/node",
            reportFiles          : "index.html",
            reportName           : "node coverage tests"
        ])

        publishHTML ( [
            allowMissing         : true,
            alwaysLinkToLastBuild: true,
            keepAll              : true,
            reportDir            : "reports/tests/coverage/ng",
            reportFiles          : "index.html",
            reportName           : "ng coverage tests"
        ])

    }

//    after('smoketest:demo') {
//        publishHTML([
//            allowMissing         : true,
//            alwaysLinkToLastBuild: true,
//            keepAll              : true,
//            reportDir            : "reports/tests/smoke/",
//            reportFiles          : 'index.html',
//            reportName           : 'DEMO Smoke Test'
//        ])
//    }

    afterAlways('smoketest:preview') {
        ensureHtmlReportForPublishing(
            "functional-output/tests/playwright-smoke/odhin-report",
            'xui-playwright-smoke.html',
            'PREVIEW Manage Org Smoke Test'
        )
        publishHTML([
            allowMissing         : true,
            alwaysLinkToLastBuild: true,
            keepAll              : true,
            reportDir            : "functional-output/tests/playwright-smoke/odhin-report",
            reportFiles          : 'xui-playwright-smoke.html',
            reportName           : 'PREVIEW Manage Org Smoke Test (Odhin)'
        ])

        stagePlaywrightArtifacts('functional-output/tests/playwright-smoke/stable-artifacts', 'test-results/playwright-smoke')
        publishPlaywrightJUnit('functional-output/tests/playwright-smoke/**/*junit.xml')
        steps.archiveArtifacts(
            allowEmptyArchive: true,
            artifacts: 'functional-output/tests/playwright-smoke/**'
        )

        runPlaywrightFunctionalTestsInParallel('PREVIEW')
    }

    afterAlways('smoketest:aat') {
        ensureHtmlReportForPublishing(
            "functional-output/tests/playwright-smoke/odhin-report",
            'xui-playwright-smoke.html',
            'AAT Manage Org Smoke Test'
        )
        publishHTML([
            allowMissing         : true,
            alwaysLinkToLastBuild: true,
            keepAll              : true,
            reportDir            : "functional-output/tests/playwright-smoke/odhin-report",
            reportFiles          : 'xui-playwright-smoke.html',
            reportName           : 'AAT Manage Org Smoke Test (Odhin)'
        ])

        stagePlaywrightArtifacts('functional-output/tests/playwright-smoke/stable-artifacts', 'test-results/playwright-smoke')
        publishPlaywrightJUnit('functional-output/tests/playwright-smoke/**/*junit.xml')
        steps.archiveArtifacts(
            allowEmptyArchive: true,
            artifacts: 'functional-output/tests/playwright-smoke/**'
        )

        runPlaywrightFunctionalTestsInParallel('AAT')
    }

}
